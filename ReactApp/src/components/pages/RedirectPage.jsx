import React from 'react';
import Header from '../layouts/Header';
import '../styles/stylesPages/RedirectPage.css'; // Asegúrate de ajustar la ruta según tu estructura de carpetas

const RedirectPage = () => {
    return (
        <div className="redirect-page">
            <Header />
        </div>
    );
};

export default RedirectPage;
